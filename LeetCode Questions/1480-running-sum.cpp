#include <bits/stdc++.h>
using namespace std;

void solve(vector<int> in) {
    vector<int> ret = in;
}

int main() {
    int t = 1;

    vector<int> in = {1, 2, 3, 4};

    while (t--) {
        solve(in);
    }

    return 0;
}