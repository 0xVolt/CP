#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> arr) {
    int index;

    

    return index;
}

int main() {
    int t = 1;

    while (t--) {
        solve();
    }

    return 0;
}